// const fs = require('fs');
// const path = require('path');

export default {
    // key: '2021062310041005', // 默认key
    // iv: '2021062310041005', // 默认偏移量
    // publicKey:fs.readFileSync(path.join(__dirname, './rsa_public_key.pem')),
    // privateKey:fs.readFileSync(path.join(__dirname, './rsa_private_key.pem')),
}
